<EditForm Model="@driver" OnValidSubmit="@OnValidSubmit">
    <DataAnnotationsValidator />
    <p>
        <MatTextField Label="First Name" @bind-Value="@driver.FirstName" FullWidth="true" ValidationDisabled="true" />
    </p>
    <p>
        <MatTextField Label="Last Name" @bind-Value="@driver.LastName" FullWidth="true" ValidationDisabled="true"/>
    </p>
    <div>
        <MatButton Type="submit">@ButtonText</MatButton>
    </div>
</EditForm>

@code {
    [Parameter] public Driver driver { get; set; }
    [Parameter] public string ButtonText { get; set; } = "Save";
    [Parameter] public EventCallback OnValidSubmit { get; set; }
}
